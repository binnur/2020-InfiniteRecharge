/**
 * Control panel interactions
 */

/**
 * Do we need to align robot w/ control panel before starting its actuation?
 * - For safety, we need to slow down or completely stop the robot before
 *   attempting to align with the wheel
 * - Do we need positioning information for color sensor to read correctly?
 */

 /**
  * Button to initiate 3-5 rotations
  */

  /**
   * Button to initiate color wheel positioning
   */

   /**
    * There maybe a need to do a single rotation if things don't go as planned
    * Manual button for initiating a single rotation
    */

    /**
     * What other driver data required?
     *  - FMS color data?
     *  - If the component is up or down?
     */